#!/usr/bin/env bash
# Managed by skvk-mbp/mise.toml ([dotfiles]).
#
# Roles, not per-item colours: a re-theme touches only this file. Dark is the
# only fixed requirement; the hues carry item state.
export BAR_BG=0xff0d0d0d   # opaque near-black
export FG=0xffe6e6e6       # default label
export DIM=0xff6b6b6b      # unfocused workspaces, idle pomodoro
export MUTED=0xff3a3a3a     # focused-workspace chip background
export ACCENT=0xff7aa2f7   # focused workspace
export OK=0xff9ece6a       # netbird connected, battery charging
export WARN=0xffe0af68     # battery under 20%, load above core count
export CRIT=0xfff7768e     # netbird down, battery under 10%
