# Called when you first enter the directory (or its children)
autosrc_enter() {
    echo "Activating '.venv' python env"
    source activate
}

# Called when you exit the directory
autosrc_exit() {
	deactivate
}
